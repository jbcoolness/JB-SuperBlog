<div class="container">
    <div class="row">
        <div class="col-xl">
            <% @posts.each do |post| %>
            <div class="card mb-3">
                <div class="row no-gutters">
                  <div class="col-md-3">
                    <img src="<%= post.image %>" class="card-img" alt="...">
                  </div>
                  <div class="col-md-9">
                    <div class="card-body">
                      <h2 class="card-title"> <%= post.title %> </h2>                      
                      <p class="card-text"><small class="text-muted"> Creado hace <%= time_ago_in_words(post.created_at) %> Por: <%= post.user_id %> </small></p>
                      <p class="card-text">
                        <%if post.content.length > 250 %> 
                          <%= truncate(post.content, length: 250) %>
                            <%= link_to  "Leer mas...", post, class: "btn btn-outline-primary btn-sm" %>
                          <% else %>
                            <%= post.content %>                          
                        <% end %>
                        <% if user_signed_in? %>
                            <% if current_user.id == post.user_id %>
                                <%= link_to  "Editar", edit_post_path(post), class: "btn btn-outline-secondary btn-sm" %>
                                <%= link_to  "Eliminar", post, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-outline-danger btn-sm" %>
                            <% end %>                          
                        <% end %>
                        
                      </p>
                      
                    </div>
                  </div>
                </div>
              </div>

            <% end %>


        </div>
    </div>
</div>